'use strict';

// create a function that returns it's input factorial
module.exports = function( num ) {
  let sum;
  if (typeof num === "number") {
    num.forEach( functin(val){
      return num *= val;
    });
    return num;
  }
};
